<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security-4.1.xsd">

    <http auto-config="true">
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/home" access="permitAll"/>
        <intercept-url pattern="/admin**" access="hasRole('ADMIN')"/>
        <form-login login-page="/welcome"
                    username-parameter="login"
                    password-parameter="password"

                    default-target-url="/userPage/"
                    login-processing-url="/signIn"
                    authentication-failure-url="/access_denied"/>
        <csrf/>
        <session-management>
            <concurrency-control max-sessions="1" />
        </session-management>
    </http>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               id="jdbcUserDetailsManager"
                               users-by-username-query="SELECT login, password, TRUE FROM registration WHERE login=?"
                               authorities-by-username-query="SELECT login, users_user_profile.user_profile_id FROM registration WHERE login=?"/>
        </authentication-provider>

        <authentication-provider>
            <user-service>
            <user name="root" password="root" authorities="ADMIN"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>

